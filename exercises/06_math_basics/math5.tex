% math5.tex  -- typeset real math, not text that merely looks like it
%
% Typeset exactly this sentence as the paragraph body:
%
%     The golden ratio is defined as phi = (1 + sqrt 5) / 2 ~ 1.618, a
%     constant that appears throughout nature and art.
%
% ...where phi is the symbol \varphi, the fraction uses \frac, the
% root uses \sqrt, and the approximation sign is \approx -- all inside
% a single pair of $ ... $ so it renders as real math, not a string of
% backslashes.

% I AM NOT DONE

\documentclass{article}
\begin{document}

Replace this placeholder paragraph with the sentence described above.

\end{document}
