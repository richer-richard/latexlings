% quiz1.tex  -- the gauntlet: everything from this course, at once
%
% This is the final exam: one document exercising every skill from
% packages/layout, bibliographies, and typesetting. Build a document
% containing ALL of the following:
%
%   [ ] a title block: \title{The Gauntlet}, \author{Final Exam},
%       \maketitle  (any \date is fine, even \date{})
%   [ ] one \section{Everything At Once}
%   [ ] a NESTED list: an itemize with two top-level items --
%         "Warm-up topics"   (nested sub-items: "Escaping reserved
%                              characters", "Typesetting math")
%         "Advanced topics"  (nested sub-items: "Tables", "Bibliographies")
%       i.e. a nested \begin{itemize}...\end{itemize} inside an \item
%   [ ] a sentence using escaped reserved characters, meaning exactly:
%         Sales grew by 50\% this quarter, driven almost entirely by
%         customer \#1 on our leaderboard.
%   [ ] inline math using \varphi, then a display equation:
%         The golden ratio $\varphi$ satisfies the quadratic equation
%         \[ \varphi^2 = \varphi + 1. \]
%   [ ] a 2-column booktabs mini-table (columns: Quantity, Value):
%         $\varphi$   & 1.618 \\
%         $1/\varphi$ & 0.618 \\
%
% Nothing here is new -- it's every earlier exercise, once each, in one
% file. Build it piece by piece and recompile often; you'll need
% \usepackage{amsmath} and \usepackage{booktabs}.
%
% Stuck? Press h in the watch window, or run:  latexlings hint quiz1

% I AM NOT DONE

\documentclass{article}
\usepackage{amsmath}
\usepackage{booktabs}

\begin{document}

Replace this placeholder with everything described above: title block,
section, nested list, escaped characters, math, and table.

\end{document}
