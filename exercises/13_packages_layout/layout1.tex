% layout1.tex
%
% \usepackage{name} tells LaTeX to load a FILE called name.sty from its
% search path. There's no spell-checker in that lookup -- typo the name
% and TeX reports the file itself as missing, not the package.
%
% This preamble wants the geometry package (it controls page margins).
% Fix the typo so it can find the real geometry.sty.
%
% Stuck? Press h in the watch window, or run:  latexlings hint layout1

% I AM NOT DONE

\documentclass{article}
\usepackage{geomtry}

\begin{document}

Once this compiles, the page will be ready for a report with sane
margins.

\end{document}
