% layout2.tex
%
% \usepackage takes an optional argument in [brackets] and a mandatory
% one in {braces}. Drop the braces and LaTeX doesn't wait for a group --
% it grabs just the very next single token as the whole argument.
%
% Put braces around the package name below so \usepackage gets
% "geometry", not just its first letter.
%
% Stuck? Press h in the watch window, or run:  latexlings hint layout2

% I AM NOT DONE

\documentclass{article}
\usepackage[margin=1in]geometry

\begin{document}

A properly margined page, coming right up.

\end{document}
