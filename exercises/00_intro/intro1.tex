% intro1.tex
%
% Welcome to latexlings!
%
% This exercise already compiles. Most of the others won't -- they contain
% a LaTeX error you must fix. Some instead ask you to typeset a document
% from scratch and check the rendered PDF text.
%
% The watch window recompiles this file every time you save it in vim.
% The pane title always shows which file to open next.
%
% To finish any exercise:
%   1. make it compile (and pass its output checks, if it has any)
%   2. delete the marker line just below this paragraph -- the whole line
%   3. press n in the watch window to move on
%
% Stuck? Press h in the watch window, or run:  latexlings hint intro1

% I AM NOT DONE

\documentclass{article}

\begin{document}

Welcome to \LaTeX{} practice. This document already compiles: pdflatex
reads the file, ignores every line that starts with a percent sign, and
typesets the rest into build/intro1/intro1.pdf.

Open that PDF once, just to see what you made. Then delete the marker
line above and press n.

\end{document}
