% display5.tex  -- an aligned derivation
%
% Typeset a three-step derivation of (x+1)^2, aligned on the = signs,
% using align* (unnumbered -- from amsmath, already loaded):
%
%     (x+1)^2 = (x+1)(x+1)
%             = x^2 + x + x + 1
%             = x^2 + 2x + 1
%
% Put an & right before each = so every line lines up underneath the
% first one, and separate the lines with \\.

% I AM NOT DONE

\documentclass{article}
\usepackage{amsmath}
\begin{document}

Replace this placeholder with the three-step derivation described
above, in an align* environment.

\end{document}
