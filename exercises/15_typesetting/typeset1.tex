% typeset1.tex  -- the abstract
%
% A complete mini-article: title block, an abstract, and a first
% section. Typeset exactly this:
%
%   title:    The Art of Not Overfitting
%   author:   anything you like
%             \maketitle
%
%   abstract (the `abstract` environment), this exact two-sentence text:
%     We argue that overfitting is best understood as a failure to
%     generalize rather than a failure to memorize. This note motivates
%     a simple regularization heuristic and previews the argument that
%     follows.
%
%   \section{Motivation}, this exact paragraph:
%     Modern models are large enough to memorize their training data,
%     yet we still expect them to perform well on data they have never
%     seen. Understanding why generalization happens at all is the
%     central puzzle this document addresses.
%
% \maketitle only prints what \title/\author/\date declared -- declare
% them in the preamble, before \begin{document}.
%
% Stuck? Press h in the watch window, or run:  latexlings hint typeset1

% I AM NOT DONE

\documentclass{article}

\begin{document}

Replace this placeholder with the title block, the abstract, and the
Motivation section described above.

\end{document}
