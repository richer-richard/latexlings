% typeset4.tex  -- the problem set
%
% A problem-set page: an unnumbered section heading, then three
% problems in an enumerate list. Typeset exactly this:
%
%   heading:  \section*{Problem Set 3}
%
%   1. Solve $x^2 - 5x + 6 = 0$ for $x$.
%   2. Compute $\sum_{k=1}^{10} k$.
%   3. Show that $(a+b)^2 = a^2 + 2ab + b^2$.
%
% Each problem mixes ordinary text with inline math ($...$). You'll
% need \usepackage{amsmath} for \sum with limits.
%
% Stuck? Press h in the watch window, or run:  latexlings hint typeset4

% I AM NOT DONE

\documentclass{article}

\begin{document}

Replace this placeholder with the heading and the three problems
described above.

\end{document}
